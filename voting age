#include<iostream>
#include<string>
using namespace std;
main()
{
	int age;
	cout<<"Enter your age: " ;
	cin>>age;
	if(age>=18)
	{
		cout<<"You are eligible to vote";
	}
	else
	{
	cout<< "You are uneliginle to vote: ";
	cout<< "Wait for " <<  18-age  << " year(s) to vote" ;
	
	}
	return 0;
	}
